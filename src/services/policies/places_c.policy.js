export const select = (record, user) => { if (!user) return false; return true; };
export const insert = (record, user) => { if (!user) return false; return true; };
export const update = (record, user) => { if (!user) return false; return true; };
export const remove = (record, user) => { if (!user) return false; return true; };
